.vercel

# Genova solo tiene licencia de escritorio. Este repo es público, así que los
# WOFF2 no se suben hasta que Graphicxell confirme la licencia web.
# Se regeneran desde los .otf con: pyftsubset --flavor=woff2
saleads/fuentes/
